import 'package:flutter/material.dart';

class Shoes_Page extends StatelessWidget {
  const Shoes_Page({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      
body: Text('shoes_pagess'),
    );
  }
}
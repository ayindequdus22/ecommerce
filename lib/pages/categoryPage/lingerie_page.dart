import 'package:flutter/material.dart';

class Lingerie_Page extends StatelessWidget {
  const Lingerie_Page({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      
body: Text('lingerie_pagess'),
    );
  }
}